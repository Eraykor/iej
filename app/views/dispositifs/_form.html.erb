<%= form_for(@dispositif) do |f| %>
  <% if @dispositif.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dispositif.errors.count, "error") %> prohibited this dispositif from being saved:</h2>

      <ul>
      <% @dispositif.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
